=====================================================================
  THIS FOLDER IS OUT OF DATE - DO NOT BUILD OR INSTALL FROM HERE
=====================================================================

This copy of the Blender add-on ("Blender-add-on" on GitHub) has not
been touched since 2026-08-19. Every fix made since then -- including
an entire session's worth of bug fixes on 2026-09-02/03 -- only exists
in the OTHER copy:

    D:\Projects\desktop-tutorial\plugins\blender-mossy-link

That is now the ONLY place to:
  - edit the add-on's source code
  - run build_addon.py
  - grab the built zips (from its dist\ subfolder)

Confirmed on 2026-09-03: this folder and the desktop-tutorial copy
have the exact same set of files -- nothing here is unique or
missing from the other copy. It is safe to stop using this folder
entirely.

Recommended next step: rename this whole folder to something like
"Blender addon - ARCHIVED - DO NOT USE" (or delete it once you're
comfortable it's no longer needed) so it can never be grabbed by
habit again.
=====================================================================
